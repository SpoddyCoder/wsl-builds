#!/usr/bin/env bash

printInfo "Installing Docker Desktop (Windows host)"

printInfo "Docker Desktop should be installed on the Windows host, not inside this WSL instance."
printInfo "See devops/README.md for Docker Desktop setup notes:"
printInfo "https://docs.docker.com/desktop/features/wsl/"

printInfo "Docker Desktop (Windows host) installed"
