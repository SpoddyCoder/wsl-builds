component: incomplete-required
upstream_tracking: review-fixture scenario only (no real upstream)
aggregation_notes: >-
  Deterministic offline scenario; required ids "deb-installed-version" and
  "installer-validated-staleness" — deb-installed-version is inconclusive, and
  installer-validated-staleness is missing from the checks array. Expected
  runner-derived concerns: incomplete=true,
  others false.
notes: >-
  Automated review testing only — see builds/review-fixture/README.md.
