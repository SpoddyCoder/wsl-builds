component: happy-path
upstream_tracking: review-fixture scenario only (no real upstream)
aggregation_notes: >-
  Deterministic offline scenario; required check id "happy-check" passes. Expected runner-derived
  concerns are all false.
notes: >-
  Automated review testing only — see builds/review-fixture/README.md.
