Deterministic offline scenario for incomplete concern routing.

- Required ids are `deb-installed-version` and `installer-validated-staleness`.
- `deb-installed-version` is inconclusive and `installer-validated-staleness` is missing from checks.
- Expected concerns: `incomplete=true`, others false.
