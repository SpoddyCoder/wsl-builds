Deterministic offline scenario for custom issue policy routing.

- A custom-kind issue is routed to `none` via `custom_issue_policy.routes_by_audit_check_id`.
- Expected concerns are all false (excluded from security/freshness without forcing incomplete).
