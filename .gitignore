# WSL Builds
# optional stray repo-root wsl-builds.conf (legacy); harness lives at test/docker/wsl-builds.conf
/wsl-builds.conf

# Cursor plans are generally short term things
.cursor/plans/*

# Maintainer-local rename checklist — not part of the repo
/wsl-builder-rename-plan.md